# FlowPoint Production Environment Configuration
# This file contains NON-SECRET environment variables for production
# Secrets are managed via Docker Swarm secrets (see DEPLOYMENT.md)
#
# Usage:
#   1. Copy this file: cp .env.production.example .env.production
#   2. Update values for your production environment
#   3. Source before deployment: set -a; source .env.production; set +a
#   4. Deploy: docker stack deploy -c docker-compose.yml flowpoint

# =============================================================================
# Domain Configuration
# =============================================================================
DOMAIN=flowpoint-demo.pointsolutions.me

# =============================================================================
# Additional Database Names
# =============================================================================
NOCODB_DB=nocodb
LANGFLOW_DB=langflow

# =============================================================================
# Registration & Access Control
# =============================================================================

# FlowPoint - User registration control (true/false)
# Production defaults: registration disabled, invitation-only
AP_SIGN_UP_ENABLED=false
AP_USER_INVITATION_ONLY=true

# NocoDB - Invitation-only signup
# Production defaults: invitation-only enabled
NC_INVITE_ONLY_SIGNUP=true
NC_DISABLE_EMAIL_AUTH=false

# Langflow - Disable authentication (false=enabled, true=disabled)
# Production default: authentication enabled
LANGFLOW_DISABLE_AUTH=false

# =============================================================================
# NOTE: Sensitive values are managed via Docker secrets
# =============================================================================
# The following are managed as Docker secrets (NOT in this file):
#   - postgres_db
#   - postgres_user
#   - postgres_password
#   - ap_encryption_key
#   - ap_jwt_secret
#   - ap_code_secrets_key
#
# Create secrets with: ./scripts/create-secrets.sh
